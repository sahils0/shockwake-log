# shockwake-log config: syslog monitor
# Install: sudo cp syslog.conf /etc/shockwake-log/syslog.conf

log = /var/log/syslog
webhook = https://hooks.slack.com/services/YOUR/WEBHOOK/URL
triggers = error,fail,fatal,panic,oom,segfault
window = 100
trailing = 20
incidents = /var/log/shockwake-incidents
