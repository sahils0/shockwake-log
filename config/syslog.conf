# swl config: syslog monitor
# Install: sudo cp syslog.conf /etc/swl/syslog.conf

log = /var/log/syslog
webhook = https://hooks.slack.com/services/YOUR/WEBHOOK/URL
triggers = error,fail,fatal,panic,oom,segfault
window = 100
trailing = 20
incidents = /var/log/swl-incidents
