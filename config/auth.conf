# swl config: auth log monitor
# Catches brute force attempts, failed logins, sudo abuse

log = /var/log/auth.log
webhook = https://hooks.slack.com/services/YOUR/WEBHOOK/URL
triggers = failed,invalid,refused,authentication failure,unauthorized
window = 50
trailing = 10
incidents = /var/log/swl-incidents
